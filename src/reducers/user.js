import update from 'immutability-helper';
import indexOf from 'lodash/indexOf'

import { initialState } from '../reducers'

import {

} from '../actions';

function userAuth(state = initialState.user, action) {
  switch (action.type) {

    default:
      return state
  }
}
export default userAuth
